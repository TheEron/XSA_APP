ID: XSA_APP
_schema-version: '2.0'
version: 0.0.1

modules:
 - name: myhdbmod
   type: hdb
   path: myhdbmod
   requires:
    - name: hdi-container

resources:
 - name: hdi-container
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
